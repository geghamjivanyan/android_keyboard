package com.gegham.phoneimckeyboard

/**
 * Действия для клавиш
 */
enum class KeyAction {
    DELETE,
    SWITCH_KEYBOARD,
    DOT,
    SPACE,
    ENTER,
    RETURN,      // для совместимости со старым кодом
    SHIFT,       // для совместимости со старым кодом
    SWITCH_LANGUAGE // для совместимости со старым кодом
} 